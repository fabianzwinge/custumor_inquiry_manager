server {
    listen 80;
    server_name _;

    # -----------------------
    # Frontend
    # -----------------------
    root /home/ubuntu/frontend;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
